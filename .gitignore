# ---------------------------------------------------------------
# 개인 정보 - 절대 올리면 안 된다
#
# 깃허브는 커밋 기록에 남으므로, 한 번 올리면 나중에 지워도
# 히스토리에서 되살릴 수 있다. 처음부터 넣지 않는 것이 유일한 방법이다.
#
#   config.json      비밀번호, 전화번호, 채팅방 별칭
#   relay.log        주고받은 문자 내용
#   설치결과.txt     설치한 PC 의 IP 주소
#   fail-shot.png    전송 실패 순간의 화면 (대화 내용이 찍힐 수 있다)
# ---------------------------------------------------------------
config.json
relay.log
relay.log.old
watchdog.log
설치결과.txt
fail-shot.png

# ---------------------------------------------------------------
# 실행 중 생기는 상태 파일
# ---------------------------------------------------------------
paused.marker
stopped.marker

# ---------------------------------------------------------------
# 설치할 때 각자 PC 에서 만들어지는 것
# ---------------------------------------------------------------
*.exe
*.ico
host-error.log

# ---------------------------------------------------------------
# 배포본 (Releases 에 첨부한다)
# ---------------------------------------------------------------
kakao-relay-setup/
kakao-relay-setup.zip

# ---------------------------------------------------------------
# 보관용 - 지금 쓰지 않는 옛 파일
# ---------------------------------------------------------------
_archive/
